[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "incidents-analysis"
version = "0.1.0"
description = "Reusable validation and metrics for incidents analysis"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["incidents_analysis"]
